rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Since this is for personal use only, we'll allow all access
    // You can restrict this later if needed
    
    match /hourly_tracking/{document=**} {
      allow read: if true;
      allow write: if true;
    }
    
    match /daily_reflections/{document=**} {
      allow read: if true;
      allow write: if true;
    }
    
    match /form_questions/{document=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}